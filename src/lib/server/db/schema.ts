/**
 * Drizzle schema — placeholder.
 *
 * The full schema lands in lesson 020. This file exists now so the db
 * client (src/lib/server/db/index.ts) can import from it without a
 * circular chicken-and-egg problem. The empty export is deliberate.
 */
export {};
